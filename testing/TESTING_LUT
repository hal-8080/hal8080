# TESTUNIT
#     - ALU
#     - MEMORY 
#     - DISPLAY 
#     - BRANCH 
#     - SET 

testDISP
    - ST
    - DISPCL, DISPCR (Display CONST on left/right 2 displays)
    - SETHI, SETLO

testADDER
    Simple adder to process user input
    - CP, INV, AND, ADD
    - LD, ST 
    - DISPRHR (Display register on right 4 displays)
    - BZERO, BA 
    - SETHI, SETLO 

testALU
    Performs all possible ALU operations
    - AND, NAND, OR, ORN, ADD, MUL, DIV, XOR, SHIFTL, SHIFTR, INV, POW, SUB, CP, RAND
    - LD, ST 
    - DISPRHR (Display register on right 4 displays)
    - BZERO, BA, CALL, RET
    - SETHI, SETLO 

testDEBUG
    Used to test debug mode with ADD and BA instruction
    - CP, ADD
    - BA